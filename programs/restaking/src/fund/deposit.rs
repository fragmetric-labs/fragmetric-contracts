use anchor_lang::prelude::*;

use crate::structs::Fund;

impl Fund {
    pub fn deposit_lst(
        &mut self,
        amount: u64,
    ) -> Result<()> {
        Ok(())
    }

    pub fn deposit_sol(
        &mut self,
        amount: u64,
    ) -> Result<()> {
        Ok(())
    }
}
