/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { DRIFT_VAULTS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const RESET_FUEL_SEASON_DISCRIMINATOR = new Uint8Array([
  199, 122, 192, 255, 32, 99, 63, 200,
]);

export function getResetFuelSeasonDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RESET_FUEL_SEASON_DISCRIMINATOR
  );
}

export type ResetFuelSeasonInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountVaultDepositor extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountDriftUserStats extends string | AccountMeta<string> = string,
  TAccountDriftState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountVaultDepositor extends string
        ? WritableAccount<TAccountVaultDepositor>
        : TAccountVaultDepositor,
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountDriftUserStats extends string
        ? WritableAccount<TAccountDriftUserStats>
        : TAccountDriftUserStats,
      TAccountDriftState extends string
        ? ReadonlyAccount<TAccountDriftState>
        : TAccountDriftState,
      ...TRemainingAccounts,
    ]
  >;

export type ResetFuelSeasonInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ResetFuelSeasonInstructionDataArgs = {};

export function getResetFuelSeasonInstructionDataEncoder(): FixedSizeEncoder<ResetFuelSeasonInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RESET_FUEL_SEASON_DISCRIMINATOR })
  );
}

export function getResetFuelSeasonInstructionDataDecoder(): FixedSizeDecoder<ResetFuelSeasonInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getResetFuelSeasonInstructionDataCodec(): FixedSizeCodec<
  ResetFuelSeasonInstructionDataArgs,
  ResetFuelSeasonInstructionData
> {
  return combineCodec(
    getResetFuelSeasonInstructionDataEncoder(),
    getResetFuelSeasonInstructionDataDecoder()
  );
}

export type ResetFuelSeasonInput<
  TAccountVault extends string = string,
  TAccountVaultDepositor extends string = string,
  TAccountAdmin extends string = string,
  TAccountDriftUserStats extends string = string,
  TAccountDriftState extends string = string,
> = {
  vault: Address<TAccountVault>;
  vaultDepositor: Address<TAccountVaultDepositor>;
  admin: TransactionSigner<TAccountAdmin>;
  driftUserStats: Address<TAccountDriftUserStats>;
  driftState: Address<TAccountDriftState>;
};

export function getResetFuelSeasonInstruction<
  TAccountVault extends string,
  TAccountVaultDepositor extends string,
  TAccountAdmin extends string,
  TAccountDriftUserStats extends string,
  TAccountDriftState extends string,
  TProgramAddress extends Address = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
>(
  input: ResetFuelSeasonInput<
    TAccountVault,
    TAccountVaultDepositor,
    TAccountAdmin,
    TAccountDriftUserStats,
    TAccountDriftState
  >,
  config?: { programAddress?: TProgramAddress }
): ResetFuelSeasonInstruction<
  TProgramAddress,
  TAccountVault,
  TAccountVaultDepositor,
  TAccountAdmin,
  TAccountDriftUserStats,
  TAccountDriftState
> {
  // Program address.
  const programAddress = config?.programAddress ?? DRIFT_VAULTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: true },
    vaultDepositor: { value: input.vaultDepositor ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: false },
    driftUserStats: { value: input.driftUserStats ?? null, isWritable: true },
    driftState: { value: input.driftState ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.vaultDepositor),
      getAccountMeta(accounts.admin),
      getAccountMeta(accounts.driftUserStats),
      getAccountMeta(accounts.driftState),
    ],
    data: getResetFuelSeasonInstructionDataEncoder().encode({}),
    programAddress,
  } as ResetFuelSeasonInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountVaultDepositor,
    TAccountAdmin,
    TAccountDriftUserStats,
    TAccountDriftState
  >);
}

export type ParsedResetFuelSeasonInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
    vaultDepositor: TAccountMetas[1];
    admin: TAccountMetas[2];
    driftUserStats: TAccountMetas[3];
    driftState: TAccountMetas[4];
  };
  data: ResetFuelSeasonInstructionData;
};

export function parseResetFuelSeasonInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedResetFuelSeasonInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vault: getNextAccount(),
      vaultDepositor: getNextAccount(),
      admin: getNextAccount(),
      driftUserStats: getNextAccount(),
      driftState: getNextAccount(),
    },
    data: getResetFuelSeasonInstructionDataDecoder().decode(instruction.data),
  };
}
