/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { DRIFT_VAULTS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const APPLY_PROFIT_SHARE_DISCRIMINATOR = new Uint8Array([
  112, 235, 54, 165, 178, 81, 25, 10,
]);

export function getApplyProfitShareDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPLY_PROFIT_SHARE_DISCRIMINATOR
  );
}

export type ApplyProfitShareInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountVaultDepositor extends string | AccountMeta<string> = string,
  TAccountManager extends string | AccountMeta<string> = string,
  TAccountDriftUserStats extends string | AccountMeta<string> = string,
  TAccountDriftUser extends string | AccountMeta<string> = string,
  TAccountDriftState extends string | AccountMeta<string> = string,
  TAccountDriftSigner extends string | AccountMeta<string> = string,
  TAccountDriftProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountVaultDepositor extends string
        ? WritableAccount<TAccountVaultDepositor>
        : TAccountVaultDepositor,
      TAccountManager extends string
        ? ReadonlySignerAccount<TAccountManager> &
            AccountSignerMeta<TAccountManager>
        : TAccountManager,
      TAccountDriftUserStats extends string
        ? WritableAccount<TAccountDriftUserStats>
        : TAccountDriftUserStats,
      TAccountDriftUser extends string
        ? WritableAccount<TAccountDriftUser>
        : TAccountDriftUser,
      TAccountDriftState extends string
        ? ReadonlyAccount<TAccountDriftState>
        : TAccountDriftState,
      TAccountDriftSigner extends string
        ? ReadonlyAccount<TAccountDriftSigner>
        : TAccountDriftSigner,
      TAccountDriftProgram extends string
        ? ReadonlyAccount<TAccountDriftProgram>
        : TAccountDriftProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ApplyProfitShareInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ApplyProfitShareInstructionDataArgs = {};

export function getApplyProfitShareInstructionDataEncoder(): FixedSizeEncoder<ApplyProfitShareInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: APPLY_PROFIT_SHARE_DISCRIMINATOR })
  );
}

export function getApplyProfitShareInstructionDataDecoder(): FixedSizeDecoder<ApplyProfitShareInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getApplyProfitShareInstructionDataCodec(): FixedSizeCodec<
  ApplyProfitShareInstructionDataArgs,
  ApplyProfitShareInstructionData
> {
  return combineCodec(
    getApplyProfitShareInstructionDataEncoder(),
    getApplyProfitShareInstructionDataDecoder()
  );
}

export type ApplyProfitShareInput<
  TAccountVault extends string = string,
  TAccountVaultDepositor extends string = string,
  TAccountManager extends string = string,
  TAccountDriftUserStats extends string = string,
  TAccountDriftUser extends string = string,
  TAccountDriftState extends string = string,
  TAccountDriftSigner extends string = string,
  TAccountDriftProgram extends string = string,
> = {
  vault: Address<TAccountVault>;
  vaultDepositor: Address<TAccountVaultDepositor>;
  manager: TransactionSigner<TAccountManager>;
  driftUserStats: Address<TAccountDriftUserStats>;
  driftUser: Address<TAccountDriftUser>;
  driftState: Address<TAccountDriftState>;
  driftSigner: Address<TAccountDriftSigner>;
  driftProgram: Address<TAccountDriftProgram>;
};

export function getApplyProfitShareInstruction<
  TAccountVault extends string,
  TAccountVaultDepositor extends string,
  TAccountManager extends string,
  TAccountDriftUserStats extends string,
  TAccountDriftUser extends string,
  TAccountDriftState extends string,
  TAccountDriftSigner extends string,
  TAccountDriftProgram extends string,
  TProgramAddress extends Address = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
>(
  input: ApplyProfitShareInput<
    TAccountVault,
    TAccountVaultDepositor,
    TAccountManager,
    TAccountDriftUserStats,
    TAccountDriftUser,
    TAccountDriftState,
    TAccountDriftSigner,
    TAccountDriftProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ApplyProfitShareInstruction<
  TProgramAddress,
  TAccountVault,
  TAccountVaultDepositor,
  TAccountManager,
  TAccountDriftUserStats,
  TAccountDriftUser,
  TAccountDriftState,
  TAccountDriftSigner,
  TAccountDriftProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DRIFT_VAULTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: true },
    vaultDepositor: { value: input.vaultDepositor ?? null, isWritable: true },
    manager: { value: input.manager ?? null, isWritable: false },
    driftUserStats: { value: input.driftUserStats ?? null, isWritable: true },
    driftUser: { value: input.driftUser ?? null, isWritable: true },
    driftState: { value: input.driftState ?? null, isWritable: false },
    driftSigner: { value: input.driftSigner ?? null, isWritable: false },
    driftProgram: { value: input.driftProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.vaultDepositor),
      getAccountMeta(accounts.manager),
      getAccountMeta(accounts.driftUserStats),
      getAccountMeta(accounts.driftUser),
      getAccountMeta(accounts.driftState),
      getAccountMeta(accounts.driftSigner),
      getAccountMeta(accounts.driftProgram),
    ],
    data: getApplyProfitShareInstructionDataEncoder().encode({}),
    programAddress,
  } as ApplyProfitShareInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountVaultDepositor,
    TAccountManager,
    TAccountDriftUserStats,
    TAccountDriftUser,
    TAccountDriftState,
    TAccountDriftSigner,
    TAccountDriftProgram
  >);
}

export type ParsedApplyProfitShareInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
    vaultDepositor: TAccountMetas[1];
    manager: TAccountMetas[2];
    driftUserStats: TAccountMetas[3];
    driftUser: TAccountMetas[4];
    driftState: TAccountMetas[5];
    driftSigner: TAccountMetas[6];
    driftProgram: TAccountMetas[7];
  };
  data: ApplyProfitShareInstructionData;
};

export function parseApplyProfitShareInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedApplyProfitShareInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vault: getNextAccount(),
      vaultDepositor: getNextAccount(),
      manager: getNextAccount(),
      driftUserStats: getNextAccount(),
      driftUser: getNextAccount(),
      driftState: getNextAccount(),
      driftSigner: getNextAccount(),
      driftProgram: getNextAccount(),
    },
    data: getApplyProfitShareInstructionDataDecoder().decode(instruction.data),
  };
}
