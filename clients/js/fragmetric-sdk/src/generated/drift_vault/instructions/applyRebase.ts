/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type WritableAccount,
} from '@solana/kit';
import { DRIFT_VAULTS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const APPLY_REBASE_DISCRIMINATOR = new Uint8Array([
  161, 115, 9, 131, 136, 29, 147, 155,
]);

export function getApplyRebaseDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPLY_REBASE_DISCRIMINATOR
  );
}

export type ApplyRebaseInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountVaultDepositor extends string | AccountMeta<string> = string,
  TAccountDriftUser extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountVaultDepositor extends string
        ? WritableAccount<TAccountVaultDepositor>
        : TAccountVaultDepositor,
      TAccountDriftUser extends string
        ? WritableAccount<TAccountDriftUser>
        : TAccountDriftUser,
      ...TRemainingAccounts,
    ]
  >;

export type ApplyRebaseInstructionData = { discriminator: ReadonlyUint8Array };

export type ApplyRebaseInstructionDataArgs = {};

export function getApplyRebaseInstructionDataEncoder(): FixedSizeEncoder<ApplyRebaseInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: APPLY_REBASE_DISCRIMINATOR })
  );
}

export function getApplyRebaseInstructionDataDecoder(): FixedSizeDecoder<ApplyRebaseInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getApplyRebaseInstructionDataCodec(): FixedSizeCodec<
  ApplyRebaseInstructionDataArgs,
  ApplyRebaseInstructionData
> {
  return combineCodec(
    getApplyRebaseInstructionDataEncoder(),
    getApplyRebaseInstructionDataDecoder()
  );
}

export type ApplyRebaseInput<
  TAccountVault extends string = string,
  TAccountVaultDepositor extends string = string,
  TAccountDriftUser extends string = string,
> = {
  vault: Address<TAccountVault>;
  vaultDepositor: Address<TAccountVaultDepositor>;
  driftUser: Address<TAccountDriftUser>;
};

export function getApplyRebaseInstruction<
  TAccountVault extends string,
  TAccountVaultDepositor extends string,
  TAccountDriftUser extends string,
  TProgramAddress extends Address = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
>(
  input: ApplyRebaseInput<
    TAccountVault,
    TAccountVaultDepositor,
    TAccountDriftUser
  >,
  config?: { programAddress?: TProgramAddress }
): ApplyRebaseInstruction<
  TProgramAddress,
  TAccountVault,
  TAccountVaultDepositor,
  TAccountDriftUser
> {
  // Program address.
  const programAddress = config?.programAddress ?? DRIFT_VAULTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: true },
    vaultDepositor: { value: input.vaultDepositor ?? null, isWritable: true },
    driftUser: { value: input.driftUser ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.vaultDepositor),
      getAccountMeta(accounts.driftUser),
    ],
    data: getApplyRebaseInstructionDataEncoder().encode({}),
    programAddress,
  } as ApplyRebaseInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountVaultDepositor,
    TAccountDriftUser
  >);
}

export type ParsedApplyRebaseInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
    vaultDepositor: TAccountMetas[1];
    driftUser: TAccountMetas[2];
  };
  data: ApplyRebaseInstructionData;
};

export function parseApplyRebaseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedApplyRebaseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vault: getNextAccount(),
      vaultDepositor: getNextAccount(),
      driftUser: getNextAccount(),
    },
    data: getApplyRebaseInstructionDataDecoder().decode(instruction.data),
  };
}
