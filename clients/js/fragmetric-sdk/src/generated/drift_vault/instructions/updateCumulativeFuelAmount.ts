/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { DRIFT_VAULTS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UPDATE_CUMULATIVE_FUEL_AMOUNT_DISCRIMINATOR = new Uint8Array([
  90, 71, 219, 233, 12, 81, 211, 11,
]);

export function getUpdateCumulativeFuelAmountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_CUMULATIVE_FUEL_AMOUNT_DISCRIMINATOR
  );
}

export type UpdateCumulativeFuelAmountInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountVaultDepositor extends string | AccountMeta<string> = string,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountDriftUserStats extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountVaultDepositor extends string
        ? WritableAccount<TAccountVaultDepositor>
        : TAccountVaultDepositor,
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountDriftUserStats extends string
        ? WritableAccount<TAccountDriftUserStats>
        : TAccountDriftUserStats,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateCumulativeFuelAmountInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type UpdateCumulativeFuelAmountInstructionDataArgs = {};

export function getUpdateCumulativeFuelAmountInstructionDataEncoder(): FixedSizeEncoder<UpdateCumulativeFuelAmountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: UPDATE_CUMULATIVE_FUEL_AMOUNT_DISCRIMINATOR,
    })
  );
}

export function getUpdateCumulativeFuelAmountInstructionDataDecoder(): FixedSizeDecoder<UpdateCumulativeFuelAmountInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getUpdateCumulativeFuelAmountInstructionDataCodec(): FixedSizeCodec<
  UpdateCumulativeFuelAmountInstructionDataArgs,
  UpdateCumulativeFuelAmountInstructionData
> {
  return combineCodec(
    getUpdateCumulativeFuelAmountInstructionDataEncoder(),
    getUpdateCumulativeFuelAmountInstructionDataDecoder()
  );
}

export type UpdateCumulativeFuelAmountInput<
  TAccountVault extends string = string,
  TAccountVaultDepositor extends string = string,
  TAccountSigner extends string = string,
  TAccountDriftUserStats extends string = string,
> = {
  vault: Address<TAccountVault>;
  vaultDepositor: Address<TAccountVaultDepositor>;
  signer: TransactionSigner<TAccountSigner>;
  driftUserStats: Address<TAccountDriftUserStats>;
};

export function getUpdateCumulativeFuelAmountInstruction<
  TAccountVault extends string,
  TAccountVaultDepositor extends string,
  TAccountSigner extends string,
  TAccountDriftUserStats extends string,
  TProgramAddress extends Address = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
>(
  input: UpdateCumulativeFuelAmountInput<
    TAccountVault,
    TAccountVaultDepositor,
    TAccountSigner,
    TAccountDriftUserStats
  >,
  config?: { programAddress?: TProgramAddress }
): UpdateCumulativeFuelAmountInstruction<
  TProgramAddress,
  TAccountVault,
  TAccountVaultDepositor,
  TAccountSigner,
  TAccountDriftUserStats
> {
  // Program address.
  const programAddress = config?.programAddress ?? DRIFT_VAULTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: true },
    vaultDepositor: { value: input.vaultDepositor ?? null, isWritable: true },
    signer: { value: input.signer ?? null, isWritable: false },
    driftUserStats: { value: input.driftUserStats ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.vaultDepositor),
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.driftUserStats),
    ],
    data: getUpdateCumulativeFuelAmountInstructionDataEncoder().encode({}),
    programAddress,
  } as UpdateCumulativeFuelAmountInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountVaultDepositor,
    TAccountSigner,
    TAccountDriftUserStats
  >);
}

export type ParsedUpdateCumulativeFuelAmountInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
    vaultDepositor: TAccountMetas[1];
    signer: TAccountMetas[2];
    driftUserStats: TAccountMetas[3];
  };
  data: UpdateCumulativeFuelAmountInstructionData;
};

export function parseUpdateCumulativeFuelAmountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateCumulativeFuelAmountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vault: getNextAccount(),
      vaultDepositor: getNextAccount(),
      signer: getNextAccount(),
      driftUserStats: getNextAccount(),
    },
    data: getUpdateCumulativeFuelAmountInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
