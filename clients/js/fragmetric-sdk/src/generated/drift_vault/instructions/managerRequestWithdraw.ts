/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { DRIFT_VAULTS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getWithdrawUnitDecoder,
  getWithdrawUnitEncoder,
  type WithdrawUnit,
  type WithdrawUnitArgs,
} from '../types';

export const MANAGER_REQUEST_WITHDRAW_DISCRIMINATOR = new Uint8Array([
  10, 238, 194, 232, 76, 55, 68, 4,
]);

export function getManagerRequestWithdrawDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MANAGER_REQUEST_WITHDRAW_DISCRIMINATOR
  );
}

export type ManagerRequestWithdrawInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountManager extends string | AccountMeta<string> = string,
  TAccountDriftUserStats extends string | AccountMeta<string> = string,
  TAccountDriftUser extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountManager extends string
        ? ReadonlySignerAccount<TAccountManager> &
            AccountSignerMeta<TAccountManager>
        : TAccountManager,
      TAccountDriftUserStats extends string
        ? ReadonlyAccount<TAccountDriftUserStats>
        : TAccountDriftUserStats,
      TAccountDriftUser extends string
        ? ReadonlyAccount<TAccountDriftUser>
        : TAccountDriftUser,
      ...TRemainingAccounts,
    ]
  >;

export type ManagerRequestWithdrawInstructionData = {
  discriminator: ReadonlyUint8Array;
  withdrawAmount: bigint;
  withdrawUnit: WithdrawUnit;
};

export type ManagerRequestWithdrawInstructionDataArgs = {
  withdrawAmount: number | bigint;
  withdrawUnit: WithdrawUnitArgs;
};

export function getManagerRequestWithdrawInstructionDataEncoder(): FixedSizeEncoder<ManagerRequestWithdrawInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['withdrawAmount', getU64Encoder()],
      ['withdrawUnit', getWithdrawUnitEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: MANAGER_REQUEST_WITHDRAW_DISCRIMINATOR,
    })
  );
}

export function getManagerRequestWithdrawInstructionDataDecoder(): FixedSizeDecoder<ManagerRequestWithdrawInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['withdrawAmount', getU64Decoder()],
    ['withdrawUnit', getWithdrawUnitDecoder()],
  ]);
}

export function getManagerRequestWithdrawInstructionDataCodec(): FixedSizeCodec<
  ManagerRequestWithdrawInstructionDataArgs,
  ManagerRequestWithdrawInstructionData
> {
  return combineCodec(
    getManagerRequestWithdrawInstructionDataEncoder(),
    getManagerRequestWithdrawInstructionDataDecoder()
  );
}

export type ManagerRequestWithdrawInput<
  TAccountVault extends string = string,
  TAccountManager extends string = string,
  TAccountDriftUserStats extends string = string,
  TAccountDriftUser extends string = string,
> = {
  vault: Address<TAccountVault>;
  manager: TransactionSigner<TAccountManager>;
  driftUserStats: Address<TAccountDriftUserStats>;
  driftUser: Address<TAccountDriftUser>;
  withdrawAmount: ManagerRequestWithdrawInstructionDataArgs['withdrawAmount'];
  withdrawUnit: ManagerRequestWithdrawInstructionDataArgs['withdrawUnit'];
};

export function getManagerRequestWithdrawInstruction<
  TAccountVault extends string,
  TAccountManager extends string,
  TAccountDriftUserStats extends string,
  TAccountDriftUser extends string,
  TProgramAddress extends Address = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
>(
  input: ManagerRequestWithdrawInput<
    TAccountVault,
    TAccountManager,
    TAccountDriftUserStats,
    TAccountDriftUser
  >,
  config?: { programAddress?: TProgramAddress }
): ManagerRequestWithdrawInstruction<
  TProgramAddress,
  TAccountVault,
  TAccountManager,
  TAccountDriftUserStats,
  TAccountDriftUser
> {
  // Program address.
  const programAddress = config?.programAddress ?? DRIFT_VAULTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: true },
    manager: { value: input.manager ?? null, isWritable: false },
    driftUserStats: { value: input.driftUserStats ?? null, isWritable: false },
    driftUser: { value: input.driftUser ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.manager),
      getAccountMeta(accounts.driftUserStats),
      getAccountMeta(accounts.driftUser),
    ],
    data: getManagerRequestWithdrawInstructionDataEncoder().encode(
      args as ManagerRequestWithdrawInstructionDataArgs
    ),
    programAddress,
  } as ManagerRequestWithdrawInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountManager,
    TAccountDriftUserStats,
    TAccountDriftUser
  >);
}

export type ParsedManagerRequestWithdrawInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
    manager: TAccountMetas[1];
    driftUserStats: TAccountMetas[2];
    driftUser: TAccountMetas[3];
  };
  data: ManagerRequestWithdrawInstructionData;
};

export function parseManagerRequestWithdrawInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedManagerRequestWithdrawInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vault: getNextAccount(),
      manager: getNextAccount(),
      driftUserStats: getNextAccount(),
      driftUser: getNextAccount(),
    },
    data: getManagerRequestWithdrawInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
