/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from '@solana/kit';
import { DRIFT_VAULTS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const RESET_DELEGATE_DISCRIMINATOR = new Uint8Array([
  204, 13, 61, 153, 97, 83, 146, 98,
]);

export function getResetDelegateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RESET_DELEGATE_DISCRIMINATOR
  );
}

export type ResetDelegateInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountDriftUser extends string | AccountMeta<string> = string,
  TAccountDriftProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountDriftUser extends string
        ? WritableAccount<TAccountDriftUser>
        : TAccountDriftUser,
      TAccountDriftProgram extends string
        ? ReadonlyAccount<TAccountDriftProgram>
        : TAccountDriftProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ResetDelegateInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ResetDelegateInstructionDataArgs = {};

export function getResetDelegateInstructionDataEncoder(): FixedSizeEncoder<ResetDelegateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RESET_DELEGATE_DISCRIMINATOR })
  );
}

export function getResetDelegateInstructionDataDecoder(): FixedSizeDecoder<ResetDelegateInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getResetDelegateInstructionDataCodec(): FixedSizeCodec<
  ResetDelegateInstructionDataArgs,
  ResetDelegateInstructionData
> {
  return combineCodec(
    getResetDelegateInstructionDataEncoder(),
    getResetDelegateInstructionDataDecoder()
  );
}

export type ResetDelegateInput<
  TAccountVault extends string = string,
  TAccountDriftUser extends string = string,
  TAccountDriftProgram extends string = string,
> = {
  vault: Address<TAccountVault>;
  driftUser: Address<TAccountDriftUser>;
  driftProgram: Address<TAccountDriftProgram>;
};

export function getResetDelegateInstruction<
  TAccountVault extends string,
  TAccountDriftUser extends string,
  TAccountDriftProgram extends string,
  TProgramAddress extends Address = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
>(
  input: ResetDelegateInput<
    TAccountVault,
    TAccountDriftUser,
    TAccountDriftProgram
  >,
  config?: { programAddress?: TProgramAddress }
): ResetDelegateInstruction<
  TProgramAddress,
  TAccountVault,
  TAccountDriftUser,
  TAccountDriftProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DRIFT_VAULTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: true },
    driftUser: { value: input.driftUser ?? null, isWritable: true },
    driftProgram: { value: input.driftProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.driftUser),
      getAccountMeta(accounts.driftProgram),
    ],
    data: getResetDelegateInstructionDataEncoder().encode({}),
    programAddress,
  } as ResetDelegateInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountDriftUser,
    TAccountDriftProgram
  >);
}

export type ParsedResetDelegateInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
    driftUser: TAccountMetas[1];
    driftProgram: TAccountMetas[2];
  };
  data: ResetDelegateInstructionData;
};

export function parseResetDelegateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedResetDelegateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vault: getNextAccount(),
      driftUser: getNextAccount(),
      driftProgram: getNextAccount(),
    },
    data: getResetDelegateInstructionDataDecoder().decode(instruction.data),
  };
}
