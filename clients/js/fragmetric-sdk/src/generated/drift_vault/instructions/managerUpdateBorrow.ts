/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { DRIFT_VAULTS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const MANAGER_UPDATE_BORROW_DISCRIMINATOR = new Uint8Array([
  193, 183, 210, 205, 223, 11, 240, 138,
]);

export function getManagerUpdateBorrowDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MANAGER_UPDATE_BORROW_DISCRIMINATOR
  );
}

export type ManagerUpdateBorrowInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountManager extends string | AccountMeta<string> = string,
  TAccountDriftUserStats extends string | AccountMeta<string> = string,
  TAccountDriftUser extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountManager extends string
        ? ReadonlySignerAccount<TAccountManager> &
            AccountSignerMeta<TAccountManager>
        : TAccountManager,
      TAccountDriftUserStats extends string
        ? WritableAccount<TAccountDriftUserStats>
        : TAccountDriftUserStats,
      TAccountDriftUser extends string
        ? WritableAccount<TAccountDriftUser>
        : TAccountDriftUser,
      ...TRemainingAccounts,
    ]
  >;

export type ManagerUpdateBorrowInstructionData = {
  discriminator: ReadonlyUint8Array;
  newBorrowValue: bigint;
};

export type ManagerUpdateBorrowInstructionDataArgs = {
  newBorrowValue: number | bigint;
};

export function getManagerUpdateBorrowInstructionDataEncoder(): FixedSizeEncoder<ManagerUpdateBorrowInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['newBorrowValue', getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: MANAGER_UPDATE_BORROW_DISCRIMINATOR,
    })
  );
}

export function getManagerUpdateBorrowInstructionDataDecoder(): FixedSizeDecoder<ManagerUpdateBorrowInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['newBorrowValue', getU64Decoder()],
  ]);
}

export function getManagerUpdateBorrowInstructionDataCodec(): FixedSizeCodec<
  ManagerUpdateBorrowInstructionDataArgs,
  ManagerUpdateBorrowInstructionData
> {
  return combineCodec(
    getManagerUpdateBorrowInstructionDataEncoder(),
    getManagerUpdateBorrowInstructionDataDecoder()
  );
}

export type ManagerUpdateBorrowInput<
  TAccountVault extends string = string,
  TAccountManager extends string = string,
  TAccountDriftUserStats extends string = string,
  TAccountDriftUser extends string = string,
> = {
  vault: Address<TAccountVault>;
  manager: TransactionSigner<TAccountManager>;
  driftUserStats: Address<TAccountDriftUserStats>;
  driftUser: Address<TAccountDriftUser>;
  newBorrowValue: ManagerUpdateBorrowInstructionDataArgs['newBorrowValue'];
};

export function getManagerUpdateBorrowInstruction<
  TAccountVault extends string,
  TAccountManager extends string,
  TAccountDriftUserStats extends string,
  TAccountDriftUser extends string,
  TProgramAddress extends Address = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
>(
  input: ManagerUpdateBorrowInput<
    TAccountVault,
    TAccountManager,
    TAccountDriftUserStats,
    TAccountDriftUser
  >,
  config?: { programAddress?: TProgramAddress }
): ManagerUpdateBorrowInstruction<
  TProgramAddress,
  TAccountVault,
  TAccountManager,
  TAccountDriftUserStats,
  TAccountDriftUser
> {
  // Program address.
  const programAddress = config?.programAddress ?? DRIFT_VAULTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: true },
    manager: { value: input.manager ?? null, isWritable: false },
    driftUserStats: { value: input.driftUserStats ?? null, isWritable: true },
    driftUser: { value: input.driftUser ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.manager),
      getAccountMeta(accounts.driftUserStats),
      getAccountMeta(accounts.driftUser),
    ],
    data: getManagerUpdateBorrowInstructionDataEncoder().encode(
      args as ManagerUpdateBorrowInstructionDataArgs
    ),
    programAddress,
  } as ManagerUpdateBorrowInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountManager,
    TAccountDriftUserStats,
    TAccountDriftUser
  >);
}

export type ParsedManagerUpdateBorrowInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
    manager: TAccountMetas[1];
    driftUserStats: TAccountMetas[2];
    driftUser: TAccountMetas[3];
  };
  data: ManagerUpdateBorrowInstructionData;
};

export function parseManagerUpdateBorrowInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedManagerUpdateBorrowInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vault: getNextAccount(),
      manager: getNextAccount(),
      driftUserStats: getNextAccount(),
      driftUser: getNextAccount(),
    },
    data: getManagerUpdateBorrowInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
