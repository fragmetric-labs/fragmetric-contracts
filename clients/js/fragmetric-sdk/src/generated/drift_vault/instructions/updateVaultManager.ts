/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { DRIFT_VAULTS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UPDATE_VAULT_MANAGER_DISCRIMINATOR = new Uint8Array([
  246, 80, 162, 207, 228, 28, 133, 170,
]);

export function getUpdateVaultManagerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_VAULT_MANAGER_DISCRIMINATOR
  );
}

export type UpdateVaultManagerInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountManager extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountManager extends string
        ? ReadonlySignerAccount<TAccountManager> &
            AccountSignerMeta<TAccountManager>
        : TAccountManager,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateVaultManagerInstructionData = {
  discriminator: ReadonlyUint8Array;
  manager: Address;
};

export type UpdateVaultManagerInstructionDataArgs = { manager: Address };

export function getUpdateVaultManagerInstructionDataEncoder(): FixedSizeEncoder<UpdateVaultManagerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['manager', getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_VAULT_MANAGER_DISCRIMINATOR })
  );
}

export function getUpdateVaultManagerInstructionDataDecoder(): FixedSizeDecoder<UpdateVaultManagerInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['manager', getAddressDecoder()],
  ]);
}

export function getUpdateVaultManagerInstructionDataCodec(): FixedSizeCodec<
  UpdateVaultManagerInstructionDataArgs,
  UpdateVaultManagerInstructionData
> {
  return combineCodec(
    getUpdateVaultManagerInstructionDataEncoder(),
    getUpdateVaultManagerInstructionDataDecoder()
  );
}

export type UpdateVaultManagerInput<
  TAccountVault extends string = string,
  TAccountManager extends string = string,
> = {
  vault: Address<TAccountVault>;
  manager: TransactionSigner<TAccountManager>;
  managerArg: UpdateVaultManagerInstructionDataArgs['manager'];
};

export function getUpdateVaultManagerInstruction<
  TAccountVault extends string,
  TAccountManager extends string,
  TProgramAddress extends Address = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
>(
  input: UpdateVaultManagerInput<TAccountVault, TAccountManager>,
  config?: { programAddress?: TProgramAddress }
): UpdateVaultManagerInstruction<
  TProgramAddress,
  TAccountVault,
  TAccountManager
> {
  // Program address.
  const programAddress = config?.programAddress ?? DRIFT_VAULTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: true },
    manager: { value: input.manager ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input, manager: input.managerArg };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.manager),
    ],
    data: getUpdateVaultManagerInstructionDataEncoder().encode(
      args as UpdateVaultManagerInstructionDataArgs
    ),
    programAddress,
  } as UpdateVaultManagerInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountManager
  >);
}

export type ParsedUpdateVaultManagerInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
    manager: TAccountMetas[1];
  };
  data: UpdateVaultManagerInstructionData;
};

export function parseUpdateVaultManagerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateVaultManagerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { vault: getNextAccount(), manager: getNextAccount() },
    data: getUpdateVaultManagerInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
