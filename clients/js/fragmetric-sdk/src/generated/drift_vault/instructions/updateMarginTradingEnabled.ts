/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { DRIFT_VAULTS_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const UPDATE_MARGIN_TRADING_ENABLED_DISCRIMINATOR = new Uint8Array([
  244, 34, 229, 140, 91, 65, 200, 67,
]);

export function getUpdateMarginTradingEnabledDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_MARGIN_TRADING_ENABLED_DISCRIMINATOR
  );
}

export type UpdateMarginTradingEnabledInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountManager extends string | AccountMeta<string> = string,
  TAccountDriftUser extends string | AccountMeta<string> = string,
  TAccountDriftProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountManager extends string
        ? ReadonlySignerAccount<TAccountManager> &
            AccountSignerMeta<TAccountManager>
        : TAccountManager,
      TAccountDriftUser extends string
        ? WritableAccount<TAccountDriftUser>
        : TAccountDriftUser,
      TAccountDriftProgram extends string
        ? ReadonlyAccount<TAccountDriftProgram>
        : TAccountDriftProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateMarginTradingEnabledInstructionData = {
  discriminator: ReadonlyUint8Array;
  enabled: boolean;
};

export type UpdateMarginTradingEnabledInstructionDataArgs = {
  enabled: boolean;
};

export function getUpdateMarginTradingEnabledInstructionDataEncoder(): FixedSizeEncoder<UpdateMarginTradingEnabledInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['enabled', getBooleanEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_MARGIN_TRADING_ENABLED_DISCRIMINATOR,
    })
  );
}

export function getUpdateMarginTradingEnabledInstructionDataDecoder(): FixedSizeDecoder<UpdateMarginTradingEnabledInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['enabled', getBooleanDecoder()],
  ]);
}

export function getUpdateMarginTradingEnabledInstructionDataCodec(): FixedSizeCodec<
  UpdateMarginTradingEnabledInstructionDataArgs,
  UpdateMarginTradingEnabledInstructionData
> {
  return combineCodec(
    getUpdateMarginTradingEnabledInstructionDataEncoder(),
    getUpdateMarginTradingEnabledInstructionDataDecoder()
  );
}

export type UpdateMarginTradingEnabledInput<
  TAccountVault extends string = string,
  TAccountManager extends string = string,
  TAccountDriftUser extends string = string,
  TAccountDriftProgram extends string = string,
> = {
  vault: Address<TAccountVault>;
  manager: TransactionSigner<TAccountManager>;
  driftUser: Address<TAccountDriftUser>;
  driftProgram: Address<TAccountDriftProgram>;
  enabled: UpdateMarginTradingEnabledInstructionDataArgs['enabled'];
};

export function getUpdateMarginTradingEnabledInstruction<
  TAccountVault extends string,
  TAccountManager extends string,
  TAccountDriftUser extends string,
  TAccountDriftProgram extends string,
  TProgramAddress extends Address = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
>(
  input: UpdateMarginTradingEnabledInput<
    TAccountVault,
    TAccountManager,
    TAccountDriftUser,
    TAccountDriftProgram
  >,
  config?: { programAddress?: TProgramAddress }
): UpdateMarginTradingEnabledInstruction<
  TProgramAddress,
  TAccountVault,
  TAccountManager,
  TAccountDriftUser,
  TAccountDriftProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DRIFT_VAULTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vault: { value: input.vault ?? null, isWritable: true },
    manager: { value: input.manager ?? null, isWritable: false },
    driftUser: { value: input.driftUser ?? null, isWritable: true },
    driftProgram: { value: input.driftProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.manager),
      getAccountMeta(accounts.driftUser),
      getAccountMeta(accounts.driftProgram),
    ],
    data: getUpdateMarginTradingEnabledInstructionDataEncoder().encode(
      args as UpdateMarginTradingEnabledInstructionDataArgs
    ),
    programAddress,
  } as UpdateMarginTradingEnabledInstruction<
    TProgramAddress,
    TAccountVault,
    TAccountManager,
    TAccountDriftUser,
    TAccountDriftProgram
  >);
}

export type ParsedUpdateMarginTradingEnabledInstruction<
  TProgram extends string = typeof DRIFT_VAULTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vault: TAccountMetas[0];
    manager: TAccountMetas[1];
    driftUser: TAccountMetas[2];
    driftProgram: TAccountMetas[3];
  };
  data: UpdateMarginTradingEnabledInstructionData;
};

export function parseUpdateMarginTradingEnabledInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateMarginTradingEnabledInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vault: getNextAccount(),
      manager: getNextAccount(),
      driftUser: getNextAccount(),
      driftProgram: getNextAccount(),
    },
    data: getUpdateMarginTradingEnabledInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
