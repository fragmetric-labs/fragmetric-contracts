/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';
import {
  getUserRewardPoolDecoder,
  getUserRewardPoolEncoder,
  type UserRewardPool,
  type UserRewardPoolArgs,
} from '../types';

export const USER_REWARD_ACCOUNT_DISCRIMINATOR = new Uint8Array([
  55, 245, 122, 238, 147, 89, 164, 198,
]);

export function getUserRewardAccountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    USER_REWARD_ACCOUNT_DISCRIMINATOR
  );
}

export type UserRewardAccount = {
  discriminator: ReadonlyUint8Array;
  dataVersion: number;
  bump: number;
  receiptTokenMint: Address;
  user: Address;
  /**
   * previous fields:
   * num_user_reward_pools: u8,
   * max_user_reward_pools: u8,
   */
  padding: ReadonlyUint8Array;
  reserved: ReadonlyUint8Array;
  baseUserRewardPool: UserRewardPool;
  bonusUserRewardPool: UserRewardPool;
  reserved2: ReadonlyUint8Array;
  delegate: Address;
  reserved3: ReadonlyUint8Array;
};

export type UserRewardAccountArgs = {
  dataVersion: number;
  bump: number;
  receiptTokenMint: Address;
  user: Address;
  /**
   * previous fields:
   * num_user_reward_pools: u8,
   * max_user_reward_pools: u8,
   */
  padding: ReadonlyUint8Array;
  reserved: ReadonlyUint8Array;
  baseUserRewardPool: UserRewardPoolArgs;
  bonusUserRewardPool: UserRewardPoolArgs;
  reserved2: ReadonlyUint8Array;
  delegate: Address;
  reserved3: ReadonlyUint8Array;
};

export function getUserRewardAccountEncoder(): FixedSizeEncoder<UserRewardAccountArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['dataVersion', getU16Encoder()],
      ['bump', getU8Encoder()],
      ['receiptTokenMint', getAddressEncoder()],
      ['user', getAddressEncoder()],
      ['padding', fixEncoderSize(getBytesEncoder(), 2)],
      ['reserved', fixEncoderSize(getBytesEncoder(), 11)],
      ['baseUserRewardPool', getUserRewardPoolEncoder()],
      ['bonusUserRewardPool', getUserRewardPoolEncoder()],
      ['reserved2', fixEncoderSize(getBytesEncoder(), 1040)],
      ['delegate', getAddressEncoder()],
      ['reserved3', fixEncoderSize(getBytesEncoder(), 1008)],
    ]),
    (value) => ({ ...value, discriminator: USER_REWARD_ACCOUNT_DISCRIMINATOR })
  );
}

export function getUserRewardAccountDecoder(): FixedSizeDecoder<UserRewardAccount> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['dataVersion', getU16Decoder()],
    ['bump', getU8Decoder()],
    ['receiptTokenMint', getAddressDecoder()],
    ['user', getAddressDecoder()],
    ['padding', fixDecoderSize(getBytesDecoder(), 2)],
    ['reserved', fixDecoderSize(getBytesDecoder(), 11)],
    ['baseUserRewardPool', getUserRewardPoolDecoder()],
    ['bonusUserRewardPool', getUserRewardPoolDecoder()],
    ['reserved2', fixDecoderSize(getBytesDecoder(), 1040)],
    ['delegate', getAddressDecoder()],
    ['reserved3', fixDecoderSize(getBytesDecoder(), 1008)],
  ]);
}

export function getUserRewardAccountCodec(): FixedSizeCodec<
  UserRewardAccountArgs,
  UserRewardAccount
> {
  return combineCodec(
    getUserRewardAccountEncoder(),
    getUserRewardAccountDecoder()
  );
}

export function decodeUserRewardAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>
): Account<UserRewardAccount, TAddress>;
export function decodeUserRewardAccount<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>
): MaybeAccount<UserRewardAccount, TAddress>;
export function decodeUserRewardAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>
):
  | Account<UserRewardAccount, TAddress>
  | MaybeAccount<UserRewardAccount, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getUserRewardAccountDecoder()
  );
}

export async function fetchUserRewardAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<Account<UserRewardAccount, TAddress>> {
  const maybeAccount = await fetchMaybeUserRewardAccount(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeUserRewardAccount<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig
): Promise<MaybeAccount<UserRewardAccount, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeUserRewardAccount(maybeAccount);
}

export async function fetchAllUserRewardAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<Account<UserRewardAccount>[]> {
  const maybeAccounts = await fetchAllMaybeUserRewardAccount(
    rpc,
    addresses,
    config
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeUserRewardAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig
): Promise<MaybeAccount<UserRewardAccount>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeUserRewardAccount(maybeAccount)
  );
}

export function getUserRewardAccountSize(): number {
  return 4248;
}
