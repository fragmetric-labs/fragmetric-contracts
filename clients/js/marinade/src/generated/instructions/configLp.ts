/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/web3.js';
import { MARINADE_FINANCE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import { getFeeDecoder, getFeeEncoder, type Fee, type FeeArgs } from '../types';

export const CONFIG_LP_DISCRIMINATOR = new Uint8Array([
  10, 24, 168, 119, 86, 48, 225, 17,
]);

export function getConfigLpDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CONFIG_LP_DISCRIMINATOR);
}

export type ConfigLpInstruction<
  TProgram extends string = typeof MARINADE_FINANCE_PROGRAM_ADDRESS,
  TAccountState extends string | IAccountMeta<string> = string,
  TAccountAdminAuthority extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountState extends string
        ? WritableAccount<TAccountState>
        : TAccountState,
      TAccountAdminAuthority extends string
        ? ReadonlySignerAccount<TAccountAdminAuthority> &
            IAccountSignerMeta<TAccountAdminAuthority>
        : TAccountAdminAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type ConfigLpInstructionData = {
  discriminator: ReadonlyUint8Array;
  minFee: Option<Fee>;
  maxFee: Option<Fee>;
  liquidityTarget: Option<bigint>;
  treasuryCut: Option<Fee>;
};

export type ConfigLpInstructionDataArgs = {
  minFee: OptionOrNullable<FeeArgs>;
  maxFee: OptionOrNullable<FeeArgs>;
  liquidityTarget: OptionOrNullable<number | bigint>;
  treasuryCut: OptionOrNullable<FeeArgs>;
};

export function getConfigLpInstructionDataEncoder(): Encoder<ConfigLpInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['minFee', getOptionEncoder(getFeeEncoder())],
      ['maxFee', getOptionEncoder(getFeeEncoder())],
      ['liquidityTarget', getOptionEncoder(getU64Encoder())],
      ['treasuryCut', getOptionEncoder(getFeeEncoder())],
    ]),
    (value) => ({ ...value, discriminator: CONFIG_LP_DISCRIMINATOR })
  );
}

export function getConfigLpInstructionDataDecoder(): Decoder<ConfigLpInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['minFee', getOptionDecoder(getFeeDecoder())],
    ['maxFee', getOptionDecoder(getFeeDecoder())],
    ['liquidityTarget', getOptionDecoder(getU64Decoder())],
    ['treasuryCut', getOptionDecoder(getFeeDecoder())],
  ]);
}

export function getConfigLpInstructionDataCodec(): Codec<
  ConfigLpInstructionDataArgs,
  ConfigLpInstructionData
> {
  return combineCodec(
    getConfigLpInstructionDataEncoder(),
    getConfigLpInstructionDataDecoder()
  );
}

export type ConfigLpInput<
  TAccountState extends string = string,
  TAccountAdminAuthority extends string = string,
> = {
  state: Address<TAccountState>;
  adminAuthority: TransactionSigner<TAccountAdminAuthority>;
  minFee: ConfigLpInstructionDataArgs['minFee'];
  maxFee: ConfigLpInstructionDataArgs['maxFee'];
  liquidityTarget: ConfigLpInstructionDataArgs['liquidityTarget'];
  treasuryCut: ConfigLpInstructionDataArgs['treasuryCut'];
};

export function getConfigLpInstruction<
  TAccountState extends string,
  TAccountAdminAuthority extends string,
  TProgramAddress extends Address = typeof MARINADE_FINANCE_PROGRAM_ADDRESS,
>(
  input: ConfigLpInput<TAccountState, TAccountAdminAuthority>,
  config?: { programAddress?: TProgramAddress }
): ConfigLpInstruction<TProgramAddress, TAccountState, TAccountAdminAuthority> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MARINADE_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    state: { value: input.state ?? null, isWritable: true },
    adminAuthority: { value: input.adminAuthority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.state),
      getAccountMeta(accounts.adminAuthority),
    ],
    programAddress,
    data: getConfigLpInstructionDataEncoder().encode(
      args as ConfigLpInstructionDataArgs
    ),
  } as ConfigLpInstruction<
    TProgramAddress,
    TAccountState,
    TAccountAdminAuthority
  >;

  return instruction;
}

export type ParsedConfigLpInstruction<
  TProgram extends string = typeof MARINADE_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    state: TAccountMetas[0];
    adminAuthority: TAccountMetas[1];
  };
  data: ConfigLpInstructionData;
};

export function parseConfigLpInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedConfigLpInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      state: getNextAccount(),
      adminAuthority: getNextAccount(),
    },
    data: getConfigLpInstructionDataDecoder().decode(instruction.data),
  };
}
