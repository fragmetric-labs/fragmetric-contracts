/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/web3.js';
import { MARINADE_FINANCE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CONFIG_VALIDATOR_SYSTEM_DISCRIMINATOR = new Uint8Array([
  27, 90, 97, 209, 17, 115, 7, 40,
]);

export function getConfigValidatorSystemDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CONFIG_VALIDATOR_SYSTEM_DISCRIMINATOR
  );
}

export type ConfigValidatorSystemInstruction<
  TProgram extends string = typeof MARINADE_FINANCE_PROGRAM_ADDRESS,
  TAccountState extends string | IAccountMeta<string> = string,
  TAccountManagerAuthority extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountState extends string
        ? WritableAccount<TAccountState>
        : TAccountState,
      TAccountManagerAuthority extends string
        ? ReadonlySignerAccount<TAccountManagerAuthority> &
            IAccountSignerMeta<TAccountManagerAuthority>
        : TAccountManagerAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type ConfigValidatorSystemInstructionData = {
  discriminator: ReadonlyUint8Array;
  extraRuns: number;
};

export type ConfigValidatorSystemInstructionDataArgs = { extraRuns: number };

export function getConfigValidatorSystemInstructionDataEncoder(): Encoder<ConfigValidatorSystemInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['extraRuns', getU32Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: CONFIG_VALIDATOR_SYSTEM_DISCRIMINATOR,
    })
  );
}

export function getConfigValidatorSystemInstructionDataDecoder(): Decoder<ConfigValidatorSystemInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['extraRuns', getU32Decoder()],
  ]);
}

export function getConfigValidatorSystemInstructionDataCodec(): Codec<
  ConfigValidatorSystemInstructionDataArgs,
  ConfigValidatorSystemInstructionData
> {
  return combineCodec(
    getConfigValidatorSystemInstructionDataEncoder(),
    getConfigValidatorSystemInstructionDataDecoder()
  );
}

export type ConfigValidatorSystemInput<
  TAccountState extends string = string,
  TAccountManagerAuthority extends string = string,
> = {
  state: Address<TAccountState>;
  managerAuthority: TransactionSigner<TAccountManagerAuthority>;
  extraRuns: ConfigValidatorSystemInstructionDataArgs['extraRuns'];
};

export function getConfigValidatorSystemInstruction<
  TAccountState extends string,
  TAccountManagerAuthority extends string,
  TProgramAddress extends Address = typeof MARINADE_FINANCE_PROGRAM_ADDRESS,
>(
  input: ConfigValidatorSystemInput<TAccountState, TAccountManagerAuthority>,
  config?: { programAddress?: TProgramAddress }
): ConfigValidatorSystemInstruction<
  TProgramAddress,
  TAccountState,
  TAccountManagerAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MARINADE_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    state: { value: input.state ?? null, isWritable: true },
    managerAuthority: {
      value: input.managerAuthority ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.state),
      getAccountMeta(accounts.managerAuthority),
    ],
    programAddress,
    data: getConfigValidatorSystemInstructionDataEncoder().encode(
      args as ConfigValidatorSystemInstructionDataArgs
    ),
  } as ConfigValidatorSystemInstruction<
    TProgramAddress,
    TAccountState,
    TAccountManagerAuthority
  >;

  return instruction;
}

export type ParsedConfigValidatorSystemInstruction<
  TProgram extends string = typeof MARINADE_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    state: TAccountMetas[0];
    managerAuthority: TAccountMetas[1];
  };
  data: ConfigValidatorSystemInstructionData;
};

export function parseConfigValidatorSystemInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedConfigValidatorSystemInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      state: getNextAccount(),
      managerAuthority: getNextAccount(),
    },
    data: getConfigValidatorSystemInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
