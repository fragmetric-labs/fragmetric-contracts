/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/web3.js';
import { MARINADE_FINANCE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const CHANGE_AUTHORITY_DISCRIMINATOR = new Uint8Array([
  50, 106, 66, 104, 99, 118, 145, 88,
]);

export function getChangeAuthorityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CHANGE_AUTHORITY_DISCRIMINATOR
  );
}

export type ChangeAuthorityInstruction<
  TProgram extends string = typeof MARINADE_FINANCE_PROGRAM_ADDRESS,
  TAccountState extends string | IAccountMeta<string> = string,
  TAccountAdminAuthority extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountState extends string
        ? WritableAccount<TAccountState>
        : TAccountState,
      TAccountAdminAuthority extends string
        ? ReadonlySignerAccount<TAccountAdminAuthority> &
            IAccountSignerMeta<TAccountAdminAuthority>
        : TAccountAdminAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type ChangeAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
  admin: Option<Address>;
  validatorManager: Option<Address>;
  operationalSolAccount: Option<Address>;
  treasuryMsolAccount: Option<Address>;
  pauseAuthority: Option<Address>;
};

export type ChangeAuthorityInstructionDataArgs = {
  admin: OptionOrNullable<Address>;
  validatorManager: OptionOrNullable<Address>;
  operationalSolAccount: OptionOrNullable<Address>;
  treasuryMsolAccount: OptionOrNullable<Address>;
  pauseAuthority: OptionOrNullable<Address>;
};

export function getChangeAuthorityInstructionDataEncoder(): Encoder<ChangeAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['admin', getOptionEncoder(getAddressEncoder())],
      ['validatorManager', getOptionEncoder(getAddressEncoder())],
      ['operationalSolAccount', getOptionEncoder(getAddressEncoder())],
      ['treasuryMsolAccount', getOptionEncoder(getAddressEncoder())],
      ['pauseAuthority', getOptionEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: CHANGE_AUTHORITY_DISCRIMINATOR })
  );
}

export function getChangeAuthorityInstructionDataDecoder(): Decoder<ChangeAuthorityInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['admin', getOptionDecoder(getAddressDecoder())],
    ['validatorManager', getOptionDecoder(getAddressDecoder())],
    ['operationalSolAccount', getOptionDecoder(getAddressDecoder())],
    ['treasuryMsolAccount', getOptionDecoder(getAddressDecoder())],
    ['pauseAuthority', getOptionDecoder(getAddressDecoder())],
  ]);
}

export function getChangeAuthorityInstructionDataCodec(): Codec<
  ChangeAuthorityInstructionDataArgs,
  ChangeAuthorityInstructionData
> {
  return combineCodec(
    getChangeAuthorityInstructionDataEncoder(),
    getChangeAuthorityInstructionDataDecoder()
  );
}

export type ChangeAuthorityInput<
  TAccountState extends string = string,
  TAccountAdminAuthority extends string = string,
> = {
  state: Address<TAccountState>;
  adminAuthority: TransactionSigner<TAccountAdminAuthority>;
  admin: ChangeAuthorityInstructionDataArgs['admin'];
  validatorManager: ChangeAuthorityInstructionDataArgs['validatorManager'];
  operationalSolAccount: ChangeAuthorityInstructionDataArgs['operationalSolAccount'];
  treasuryMsolAccount: ChangeAuthorityInstructionDataArgs['treasuryMsolAccount'];
  pauseAuthority: ChangeAuthorityInstructionDataArgs['pauseAuthority'];
};

export function getChangeAuthorityInstruction<
  TAccountState extends string,
  TAccountAdminAuthority extends string,
  TProgramAddress extends Address = typeof MARINADE_FINANCE_PROGRAM_ADDRESS,
>(
  input: ChangeAuthorityInput<TAccountState, TAccountAdminAuthority>,
  config?: { programAddress?: TProgramAddress }
): ChangeAuthorityInstruction<
  TProgramAddress,
  TAccountState,
  TAccountAdminAuthority
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MARINADE_FINANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    state: { value: input.state ?? null, isWritable: true },
    adminAuthority: { value: input.adminAuthority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.state),
      getAccountMeta(accounts.adminAuthority),
    ],
    programAddress,
    data: getChangeAuthorityInstructionDataEncoder().encode(
      args as ChangeAuthorityInstructionDataArgs
    ),
  } as ChangeAuthorityInstruction<
    TProgramAddress,
    TAccountState,
    TAccountAdminAuthority
  >;

  return instruction;
}

export type ParsedChangeAuthorityInstruction<
  TProgram extends string = typeof MARINADE_FINANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    state: TAccountMetas[0];
    adminAuthority: TAccountMetas[1];
  };
  data: ChangeAuthorityInstructionData;
};

export function parseChangeAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedChangeAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      state: getNextAccount(),
      adminAuthority: getNextAccount(),
    },
    data: getChangeAuthorityInstructionDataDecoder().decode(instruction.data),
  };
}
